export enum ButtonVariant {
  TEXT = "text",
  OUTLINE = "outline",
}

export enum ButtonSize {
  SM = "sm",
  MD = "md",
  LG = "lg",
}

export enum ButtonColor {
  DEFAULT = "btn-default",
  PRIMARY = "btn-primary",
  SECONDARY = "btn-secondary",
  DANGER = "btn-danger",
}
